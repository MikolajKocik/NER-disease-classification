[project]
name = "ner-bert-app"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi[standard]",
    "transformers",
    "pandas",
    "tenacity",
    "slowapi",
    "grpcio",
    "grpcio-tools"
    "pydantic"
]